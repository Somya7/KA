{
  "name": "kerala-ayurveda-pdp-enhance",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "test": "npm run test --workspace=web",
    "lint": "npm run lint --workspace=web"
  }
}
